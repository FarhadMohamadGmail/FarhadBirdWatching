<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="farhadarts.birdwatching">

    <!-- i have added these two lines for finding my location -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SearchBirdsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".BirdObservationActivity"
            android:configChanges="keyboardHidden|orientation|screenLayout|touchscreen|screenSize" />
        <!--<activity android:name=".SearchObservations" />-->
        <!--<activity android:name=".SearchBirds" />-->
        <activity android:name=".MainActivity"/>

        <activity android:name=".BirdDetailActivity"></activity>
    </application>

</manifest>